[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "two-bank-atm"
version = "0.0.0"
description = "TWO Bank - The Worldwide Only Bank ATM Project"
authors = [{name = "israelscr-prog"}]

[tool.pytest.ini_options]
pythonpath = ["."]
